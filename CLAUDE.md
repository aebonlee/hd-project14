# [Claude Code 작업 지시서] 신규 딜러 Access Portal 등록 자동화

- 기획자: 임나연 (HD건설기계)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] — 수강생 프로젝트 기획서 14번
- 프로젝트 유형: 엑셀 검증 + 국가별 권한 결정 + 포털 입력 보조(Playwright)
- 대체 대상: **딜러가 새로 생길 때마다 손으로 반복하던 PROFILE 등록 + AUTHORIZATION 설정**

> ⚠ **이 문서는 기획서 원문이 아니라 저장소에서 되짚어 쓴 것이다.**
> 다른 프로젝트의 작업 지시서는 기획서를 먼저 받아 적은 것이지만, 이 저장소는
> 구현이 먼저 있었고 지시서가 없었다. 그래서 §1~§5 는 **README·커밋 메시지·
> 테스트에서 실제로 확인한 것만** 옮겼고, **부록 A(기획서 원문)는 비워 두었다.**
> 지어내면 다음 사람이 그것을 기획자의 말로 믿는다.

---

## 0. Claude Code에게 — 작업 방식

1. 이 도구의 값어치는 빨리 넣는 것이 아니라 **잘못 넣지 않는 것**에 있다.
   포털에 절반쯤 들어간 뒤 틀린 것을 발견하면 되돌리기가 훨씬 번거롭다.
   그래서 앞에서 거른다.
2. 규칙은 `js/logic.js` 에 모으고 `test/logic.test.js` 로 고정한다.
   화면(`js/app.js`)은 판정하지 않는다.
3. **국가별 권한은 `config/permissions.json` 하나가 정본이다.**
   나라가 늘거나 권한이 바뀌면 코드가 아니라 이 파일을 고친다.
4. **계정 정보를 다루지 않는다.** 받지도, 저장하지도, 묻지도 않는다(§3.3).
5. **사내 실데이터를 저장소에 넣지 않는다.** 예제는 `scripts/make_sample.py` 가
   만든 가짜 딜러 목록을 쓴다.

---

## 1. 한 줄 요약

딜러 등록 요청 **엑셀을 올리면 줄마다 검증하고 국가로 권한 다섯 가지를 정해**,
포털 입력칸까지 대신 채우되 **로그인과 최종 Save 는 사람이 하는** 도구.

## 2. 배경 및 현재 업무 상황

- 딜러가 새로 생길 때마다 Access Portal 에 **PROFILE 등록 + AUTHORIZATION 설정**을
  손으로 반복한다.
- 권한은 **국가에 따라** 달라지는데, 담당자가 표를 보며 다섯 항목을 하나씩 고른다.
- 엑셀을 눈으로 훑어 확인하므로, 빠진 항목이나 중복 이메일이 **포털에 들어간 뒤**
  드러나는 일이 생긴다.

## 3. 저장소에서 확인한 것

### 3.1 다섯 단계 중 무엇을 자동화했나

| 단계 | 지금 | 이 도구 |
|---|---|---|
| ① 딜러 정보 확인 | 엑셀을 눈으로 훑음 | **엑셀을 올리면 줄마다 검증** |
| ② 국가별 권한 결정 | 표를 보며 하나씩 고름 | **국가로 다섯 항목 자동 결정** |
| ③ 로그인 | 사람 | **사람** (자동화하지 않는다 — §3.3) |
| ④ 포털 입력 | 한 명씩 손으로 | 고른 딜러만 Playwright 로 대신 입력 |
| ⑤ 최종 저장 | 사람 | **사람** (그대로 둔다) |

⑤를 자동화하지 않은 것은 **기획서의 요구 그대로**다 — *"최종 Save 전 검토 단계 필요"*.

### 3.2 국가별 권한

`config/permissions.json` 하나가 정본이다.

```json
"KR": { "name": "대한민국", "dealernet": "관리자", "parts": "조회+등록",
        "ordering": "조회+등록", "mydevelon": "조회", "irw": "관리자" }
```

- 권한 항목 다섯: DealerNET · Parts Catalog · Complete Ordering · My Develon · Inventory, Retails & Warranty
- 등급 넷: 없음 · 조회 · 조회+등록 · 관리자

**매핑에 없는 국가는 기본값을 주지 않는다.** 조용히 기본 권한을 주면 잘못된 권한이
나가도 아무도 모른다. 그 국가를 표에 추가하도록 막아 세운다.

### 3.3 로그인 자동화를 만들지 않았다

사내 정책상 **로그인 자동화가 금지**되어 있고 외부 포털도 마찬가지다.
그래서 기능 자체를 만들지 않았다 — 아이디·비밀번호를 **받지도, 저장하지도, 묻지도 않는다.**

흐름: 넣을 딜러를 고른다(전부 `a` · 일부 `1,3,5` · 범위 `2-4`. **고르지 않으면 취소**) →
브라우저가 열리면 사람이 평소대로 로그인 → 준비되면 Enter → 한 명씩 멈추고
사람이 확인 후 직접 Save.

브라우저 세션만 `.portal-session/` 에 남는다. 커밋되지 않으며 공용 PC 에서는 지운다.

---

## 4. 반드시 규칙으로 막을 것 (테스트로 고정)

`test/logic.test.js` 19개가 지킨다. ★ 는 틀리면 포털에 잘못된 것이 실제로 들어가는 것.

| # | 규칙 | 어디서 |
|---|---|---|
| 1 | ★ **매핑에 없는 국가는 기본 권한을 주지 않고 막아 세운다** | `Logic.permissionsFor` |
| 2 | ★ **같은 이메일이 두 번 오면 잡는다** — 대소문자가 달라도 같은 주소 | `Logic.findDuplicates` |
| 3 | ★ 이메일·국가가 없는 표는 **딜러 목록으로 보지 않는다** | `Logic.findHeaderRow` · `mapHeader` |
| 4 | 문제 있는 줄을 **고쳐서 채우지 않는다** | `Logic.checkRow` |
| 5 | 포털이 받지 않을 이메일을 거른다 (공백·`@` 없음·한글·점 없는 도메인) | `Logic.checkEmail` |
| 6 | 멀쩡한 이메일은 통과시킨다 — **지나치게 엄격해도 안 된다** | 같음 |
| 7 | 전화번호는 **넣으면 안 될 글자만** 빼고 자릿수는 건드리지 않는다 | `Logic.normalizePhone` |
| 8 | 국가를 코드·소문자·한글 이름 어느 쪽으로 적어도 알아본다 | `Logic.normalizeCountry` |
| 9 | 모르는 국가는 **추측하지 않는다** | 같음 |
| 10 | 등급 목록에 없는 값은 받지 않는다 · 설정 파일 자체를 검사한다 | `Logic.permissionsFor` |
| 11 | 빈 이메일은 중복으로 세지 않는다 | `Logic.findDuplicates` |
| 12 | 검토가 필요한 줄은 **기본으로 꺼 둔다** | 화면 + `test/smoke.browser.js` |

### 왜 고쳐서 채우지 않는가

빠진 값을 그럴듯하게 메우면 그 줄이 **문제없는 줄처럼 보인다.**
포털에 들어간 뒤 되돌리는 것이 훨씬 번거롭다. 무엇이 문제인지 맨 오른쪽에 적고,
사람이 원본을 고쳐 다시 올리게 한다.

### 왜 전화번호 자릿수를 건드리지 않는가

나라마다 다르다. 형식을 맞춘답시고 자르거나 채우면 **연락이 닿지 않는 번호**가 된다.
넣으면 안 될 글자만 뺀다.

---

## 5. 산출물

- 빌드 없이 도는 순수 HTML/CSS/JS (GitHub Pages 배포) + `guide.html` 사용법
- `scripts/portal-fill.mjs` — 포털 입력 보조 (Playwright, 사람이 로그인·저장)
- `config/permissions.json` — 국가별 권한 정본
- `test/logic.test.js` 19개 · `test/smoke.browser.js` 21개
- `scripts/make_sample.py` — 예제 엑셀

예제 엑셀에는 **일부러 문제 있는 줄**을 섞어 두었다 — 모르는 국가, 이메일 공백,
이름 누락, 이메일 중복. 검증이 실제로 잡는지 보려면 그런 줄이 있어야 한다.

## 6. 아직 채워야 하는 것

- **포털 화면의 입력칸 이름(선택자)** — 사이트가 사외에서만 열려 비워 두었다.
  **비어 있으면 스크립트가 스스로 멈춘다** — 엉뚱한 곳에 입력하지 않도록.
  채우는 방법은 `guide.html` 4번에 있다.
- **등록 API 확인** — 포털이 API 를 직접 받는다면 화면을 흉내 내는 것보다 훨씬 안정적이다.
  화면은 개편되면 바로 깨진다.

---

## 부록 A. 기획서 원문

> **비어 있음.**
>
> 이 저장소에는 기획자(임나연)의 기획서 원문이 남아 있지 않다.
> 다른 프로젝트의 작업 지시서는 이 자리에 원문을 그대로 싣고 있고,
> 그 원문이 최종 기준 역할을 한다.
>
> 다만 README 와 커밋에 원문에서 온 것이 분명한 요구가 두 군데 남아 있다 —
> *"최종 Save 전 검토 단계 필요"* 와 *"로그인 기능은 임의로 체크하거나 선택하게
> 해야 함"*(기획자 확인 사항). 이 둘은 §3.1·§3.3 에 반영되어 있다.
>
> **채우는 방법**: 원문을 받아 이 아래에 인용(`>`)으로 그대로 붙인다.
> 붙인 뒤 §1~§4 가 원문과 어긋나는 곳이 없는지 확인한다 —
> 지금 적힌 것은 구현에서 되짚은 것이라, **기획자가 요구했지만 구현되지 않은
> 항목은 여기 없을 수 있다.**
>
> 요약하거나 다시 쓰지 말 것. 원문 그대로가 기준이다.
